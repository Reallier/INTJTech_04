# 开发环境配置

## Python 环境

推荐使用 `uv` 管理 Python 环境：

```bash
# 安装 uv
pip install uv

# 在项目目录创建虚拟环境并安装依赖
uv sync
```

## Node.js 环境

使用 npm 管理前端依赖：

```bash
npm install
npm run dev
```

## Docker 环境

安装 Docker Desktop 并确保运行正常：

```bash
docker --version
docker compose version
```
